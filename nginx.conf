#user  nobody;
worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;
    #文件上传大小
    client_max_body_size 50M;

    # gzip压缩 on:开启 off关闭(由ESA进行管理)
    gzip off;
    # 不压缩临界值，大于1K的才压缩，一般不用改
    gzip_min_length 1k;
    # 压缩缓冲区
    gzip_buffers 16 64K;
    # 压缩版本（默认1.1，前端如果是squid2.5请使用1.0）
    gzip_http_version 1.1;
    # 压缩级别，1-10，数字越大压缩的越好，时间也越长
    gzip_comp_level 5;
    # 进行压缩的文件类型
    gzip_types text/plain application/x-javascript text/css application/xml application/javascript;
    # 跟Squid等缓存服务有关，on的话会在Header里增加"Vary: Accept-Encoding"
    gzip_vary on;
    # IE6对Gzip不怎么友好，不给它Gzip了
    gzip_disable "MSIE [1-6]\.";

    server {
        listen       80;
        server_name  localhost;

        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
			try_files $uri $uri/ @router;
        }
        location ^~/api {
            rewrite ^/api/(.*)$ /$1 break;
            proxy_set_header GATEWAY_H5 'HfNLPDB8Nv6VL!sp3!';
            proxy_pass http://server-member.uupay:8081;
        }

        location ^~/test-api {
            rewrite ^/test-api/(.*)$ /$1 break;
            proxy_set_header GATEWAY_H5 'rE_c&p+BDayA^27KHx';
            proxy_pass http://server-member.ucard:8081;
        }

		location @router {
            rewrite ^(.+)$ /index.html last;
        }

        location ^~ /zh-cn/blog {
             resolver 8.8.8.8 8.8.4.4 valid=5s;
             set $backend_host "aaacn5czgb2u5bu6.mylandingpages.co";
             proxy_ssl_verify off;
             proxy_set_header Host $backend_host;
             proxy_ssl_server_name on;
             proxy_pass https://$backend_host;
             proxy_redirect ~^(http://[^/]+)(/.*)$ https://$http_host$2;
        }
        location ^~ /zh-tw/blog {
             resolver 8.8.8.8 8.8.4.4 valid=5s;
             set $backend_host "aaaf7ygbkbenjvox.mylandingpages.co";
             proxy_ssl_verify off;
             proxy_set_header Host $backend_host;
             proxy_ssl_server_name on;
             proxy_pass https://$backend_host;
             proxy_redirect ~^(http://[^/]+)(/.*)$ https://$http_host$2;
        }
        location ^~ /en/blog {
             resolver 8.8.8.8 8.8.4.4 valid=5s;
             set $backend_host "aaadwxnf23yr6bsp.mylandingpages.co";
             proxy_ssl_verify off;
             proxy_set_header Host $backend_host;
             proxy_ssl_server_name on;
             proxy_pass https://$backend_host;
             proxy_redirect ~^(http://[^/]+)(/.*)$ https://$http_host$2;
        }


        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }

}
